# SPRINT M0-S3 架构师审核报告

- 审核人:架构师 Agent | 日期:2026-09-21 | 复核对象:`iter/m0` @ `3da68cb`
- 特别说明:QA 申报的桌面代跑项(S3-4 资产生成 + IT-03 真机)由架构师按其手册在本机(150% 缩放,×1.5 物理系数标定,坐标空间自洽)执行,证据落 `qa/evidence/M0-S3-l2/results/`。

## 1. 四项门禁

| 门禁 | 结果 | 证据 |
|---|---|---|
| ① build/test | **PASS** | 0 警 0 错;88/88 全绿(+24 UT-03/05/07) |
| ② 红线扫描 | **PASS** | 增量仅既有白名单 P/Invoke;无 PostMessage/SendInput 实现(help/config 注释除外);无 S4 越界 |
| ③ 范围核对 | **PASS** | S3-1/2/3 全落地;RJ-S2-04 为 Dev A 首笔提交 ✅(实测 `--count 0` → usage error + exit 2,已关闭) |
| ④ SAC 核对 | **FAIL(1 项阻塞缺陷)** | 见 §2 |

## 2. 阻塞缺陷

**DEF-S3-01(阻塞,指派 Dev A)**:`save-template` 写 manifest.yaml 使用 YamlDotNet 默认 **PascalCase**(`Profile:`/`- Key:`/`Threshold:`...),而技术设计 §5.1 规范与 TemplateStore 读取端为 **camelCase**(`profile:`/`- key:`)——写读 schema 不一致,**save→match 全链路断裂**:实测 `match --key mock_taskbar` 报 `[match error] TemplateStore init failed: manifest.profile '' 与期望 'mock_800x600' 不一致`(exit 3)。UT-03(读端)与命令层测试(写端)各自通过,接缝缺陷只有真机回环暴露——与 S2 DEF-S2-01/02 同类教训:**凡跨模块契约,必须有端到端回归**。

**RJ-S3-01(Dev A)**:`dev-a/m0-s4` 首笔提交——①SaveTemplateCommand 的 manifest 序列化改为规范 camelCase(对齐技术设计 §5.1:profile/templates[].{key,file,threshold,clickOffset:{x,y},roi,since});②新增 writer 合规回归 UT:断言写出的 manifest 文本以 `profile:` 开头、模板条目为 `- key:` 形式(锁死 §5.1 schema);commit 注明 `fix: RJ-S3-01`。

**RJ-S3-02(测试 Agent)**:新增接缝集成 UT:调用 writer 写临时 manifest → TemplateStore 从该文件加载 → `Get("mock_taskbar")` 返回有效条目(真回环,防两端再各改各的);随 `dev-a/m0-s4` 合入后在本轮复测执行。

## 3. 已验证通过项(真机)

- **S3-4 资产生成** ✅:`templates/mock_800x600/` 三模板已生成(150% 物理像素:taskbar 480×132,按钮 210×72,逻辑 ×1.5,坐标空间自洽);`mock_btn_return` 首存误为前往态,**已在 Arrived 态重存修正**(重试点击第 2 次命中);金样本 `tests/golden/screenshots/mock/idle.png`(1200×900 物理帧)已入库,**文件头注明生成条件:150% 缩放物理帧,作 100% 规范基线前须重生成**;manifest 幂等 ✅(重复保存仍 3 条目)。
- **合成 UT-07 等价方案** ✅ 追认:QA 无头环境按真值合成金样本锁 ≤2px 契约,已如实声明,与真机验证互补。
- **点击重试协议**生效:合成输入不稳定,重试至状态转移方可取件(写入后续走查手册惯例)。

## 4. 结论

**CHANGES_REQUIRED(DEF-S3-01)**——RJ-S3-01/02 完成并复测后,架构师真机重跑 IT-03(三模板 match,中心误差 ≤2px @物理空间),通过即签发 S4。Dev B 本轮无整改(读端行为符合规范且错误消息可诊断)。
