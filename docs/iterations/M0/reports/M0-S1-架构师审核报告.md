# SPRINT M0-S1 架构师审核报告

- 审核人:架构师 Agent | 日期:2026-09-20 | 复核对象:`iter/m0` @ `5c80fe8`
- 复核方式:检出后亲自复跑 build/test、红线扫描、范围核对、SAC 核对(证据见下)

## 1. 材料齐备性

三份《任务完成报告》齐备且随分支进入 iter/m0:DevA(`8a87d11`)、DevB(`0288c23`)、QA(`5c80fe8`)。附:冲突上报表、裁决书 RJ-M0-S1、覆盖率证据(`qa/evidence/M0-S1-coverage/coverage.cobertura.xml`)。

## 2. 四项门禁结论

| 门禁 | 结果 | 复核证据 |
|---|---|---|
| ① 复跑 build/test | **PASS** | `dotnet build DH2.slnx -c Release`:0 警告 0 错误;`dotnet test`:54/54 通过(411ms),与 QA 报告一致 |
| ② 红线快扫 | **PASS** | 全仓 grep 禁止 API:唯一命中为 Core 注释中"不依赖 OpenProcess"说明;P/Invoke 仅 comctl32 #410/#412/#413(同进程自身 HWND 子类化,技术设计 §7 许可);包清单无注入/钩子类;Avalonia 已统一 11.2.7 |
| ③ 范围核对 | **PASS** | 80 文件 +6430/-15;Input/Capture/Vision 仅 csproj 骨架、App 仅壳(属 S1-1);无 S2+ 功能实现;bin/obj 未入库;MockLayout 落位 Core(跨域依赖正确归并) |
| ④ SAC 核对 | **PASS(1 项 DEFERRED)** | SAC1-1 ✓(复跑一致);SAC1-2 ✓(Core 79.24% ≥70%,MockLayoutLoader 26.7% 见 DEF-S1-01);SAC1-3 ⚠️ DEFERRED(QA headless 无法跑 GUI 走查,诚实记录);SAC1-4 ✓(双分支+集成都已推送) |

## 3. 偏离裁决(Dev A 报告 7 项)

| # | 偏离 | 裁决 |
|---|---|---|
| 1 | Frame.Image Bitmap→Mat | **接受**(理由成立:Core 不引 System.Drawing.Common;技术设计 §2.1 已同步修订) |
| 2 | MockLayoutConfig 全字段默认值 | **接受** |
| 3 | IFrameCapture 返回非空 Frame | **接受**(附注:S2 GdiCapture 对"窗口不可见/已销毁"必须返回可判别的空帧语义,写入 S2 实施注意) |
| 4 | foreground 校验前置到配置校验阶段 | **接受**(但消息文案须符合技术设计 §2.3,见 DEF-S1-02) |
| 5 | FromLParam 无符号解析 | **接受**(65535 满量程往返是 UT-01 明确要求) |
| 6 | WaitUntilAsync 谓词不接 ct | **接受**(按设计) |
| 7 | tests csproj 由 Dev A 建立 | **接受,规则澄清**:csproj 骨架属解决方案结构(合法),测试代码 .cs 归测试 Agent 独占(07 文档已增补该细则) |

## 4. 遗留缺陷与整改(带入 S2 开工,不阻塞 S1 签收)

| 编号 | 摘要 | 指派 | 落点 |
|---|---|---|---|
| DEF-S1-01 | MockLayout 族 YamlDotNet 反序列化缺陷(无参构造缺失)+ dead code;MockLayoutLoader 覆盖 26.7% | Dev A | RJ-S1-03:`dev-a/m0-s2` 首笔提交修复解析缺陷,并统一 YamlDotNet 版本(16.3.0,Directory.Packages.props 集中管理) |
| (§7 执行偏差) | MockGame 未在启动时读 mock-layout.yaml 渲染,几何硬编码在 XAML(技术设计 §7 要求运行时读取;Dev B 未事前声明,本次审核追认) | Dev B | RJ-S1-04:`dev-b/m0-s2` 首笔提交接入 MockLayoutLoader,删除硬编码几何 |
| DEF-S1-02 | ConfigValidator foreground 分支不可达且消息误导;UT 被迫放宽 | Dev A | RJ-S1-05:随 RJ-S1-03 修复(消息按技术设计 §2.3),UT 恢复原期望 |

**SAC1-3(DEFERRED)闭环条件**:S2 的 IT-01/IT-04 本就要求交互桌面会话(100% 缩放);QA 在 S2 必须于**真桌面会话**执行,IT-04 通过(raw/ui 双日志 + 状态转移)即视为 SAC1-3 闭环;**S2 审核若 SAC1-3 未闭环,则 S2 不通过**。

## 5. 非阻塞建议

- System.Drawing.Common 8.0.0 建议在 S2(其真正使用点)升到 10.x;
- OpenCvSharp4 自动升版至 4.10.0.20241107 已知悉,S3 Vision 实现时复查 ABI;
- NU1903 抑制(Avalonia 传递依赖)保留,上游修复后移除;
- `.slnx` 格式接受(.NET 10 新标准,已声明迁移路径)。

## 6. 结论

**PASS(附条件)** —— S1 交付签收;RJ-S1-03/04/05 与 SAC1-3 闭环作为 **S2 的开工前置与审核硬条件**。

## 7. 下一轮放行指令(S2)

- 统一触发语:`开始 M0-S2`
- 特别事项:①Dev A 开工先落 RJ-S1-03/05,Dev B 先落 RJ-S1-04(各为分支首笔提交);②QA 须在交互桌面会话执行 L2(否则 SAC1-3 无法闭环);③S2 范围 = Story 卡 S2-1~S2-4(Input/Capture/CLI enumerate+capture/IT-01/02),仍严禁越界 S3/S4。
