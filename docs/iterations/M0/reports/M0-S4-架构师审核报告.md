# SPRINT M0-S4 架构师审核报告(第一轮)

- 审核人:架构师 Agent | 日期:2026-09-21 | 复核对象:`iter/m0` @ `5f09df2`
- 终审方式:代码门禁复核 + 架构师真机代跑 e2e(含资产生成修复,详见 §3)

## 1. 四项门禁

| 门禁 | 结果 | 证据 |
|---|---|---|
| ① build/test | **PASS** | 0 警 0 错;121/121 全绿(+26 QA UT) |
| ② 红线全仓扫描(M0 出口 G1 预演) | **PASS** | 全仓零命中;PostMessageW 为技术设计 §3.1 白名单内声明;comctl32 子类化三件套不变 |
| ③ 范围核对 | **PASS** | delta 仅 S4 文件(click/e2e/report/IInputDriver/PostMessageDriver + 对应测试);无 M1a 特征(OCR/场景分类/锚点体系零出现) |
| ④ SAC 核对 | **FAIL(1 项阻塞缺陷)** | DEF-S4-01,见 §2 |

## 2. 阻塞缺陷

**DEF-S4-01(阻塞,指派 Dev A)**:`e2e` 的点击坐标推导存在物理/逻辑空间混用。证据链(全部在 `qa/evidence/M0-S4-l2/`):
- taskbar 匹配 score=1.0,center=(264,90)(150% 物理空间)✅;
- PostMessage 投递 (86,204)(mock-layout **逻辑**坐标)→ MockGame raw 日志 `0x0201 x=86 y=204` ✅ 投递成功;
- Avalonia ui 日志 `PointerPressed (57,136)` = (86,204)÷1.5 —— 接收端按物理→DIP 换算消息坐标;
- 按钮 DIP 区域 y∈[180,228],落点 y=136 → **按钮上方 44 DIP,未命中**,状态不转移。

**RJ-S4-01(Dev A,`dev-a/m0-s5` 分支或 `dev-a/m0-s4` 追加)**:E2eCommand 坐标推导改为帧空间——`scale = frame.Width / layout.window.width`;点击坐标 = taskbar 实测中心 + (按钮布局中心 − 任务栏布局中心) × scale。新增 UT:150% 帧(1200×900)注入 → 断言点击坐标 = (129,306);100% 帧(800×800 不可能,用 800×600)注入 → 断言 (86,204)。commit 注明 `fix: RJ-S4-01`。

**附:RJ-S4-02(测试 Agent)**:为坐标推导规则补一组合成帧接缝 UT(写死的 scale 换算断言),并入本轮复测。

## 3. 过程重大发现(已处置)

**资产污染事件(DEF-S3-02 追溯)**:S3 终审入库的三模板 + gold-idle 实为**游戏场景裁剪**——生成时游戏客户端全屏遮挡 MockGame,GDI CopyFromScreen 抓到的是屏幕上的游戏画面;且当时"保存后立即 match=1.0"是**自指验证假阳性**(模板与被匹配帧来自同一张被污染截屏)。今日 e2e 才暴露。
处置:①已按**独立内容断言规程**重生成 taskbar/btn_go/gold-idle(断言任务栏中心像素=#1E1E2E、按钮中心=#2D5BFF,全过);②mock_btn_return 保持 12:02 版本(验证过 0.9145 有效);③**规程沉淀(写入 M1a 设计输入)**:资产生成前必须独立内容断言;WGC(穿透遮挡)优先级进一步确认。

## 4. 积极结论(重要)

- **M0 核心架构问题得到肯定答案**:PostMessage → Avalonia 原始消息(raw 0x0201/0x0202)→ UI 事件(PointerPressed/Released)全链路打通,仅坐标空间推导待修——S4 的 PostMessageDriver 实现正确;
- e2e 框架(枚举→截屏→锚定→点击→轮询→证据落盘)机制完整,taskbar 锚定 score=1.0;
- report 向导(V1~V6)与自检报告草稿交付合规。

## 5. 结论

**CHANGES_REQUIRED(DEF-S4-01)**——修复 + 复测后,架构师立即重跑 e2e;PASS 即 SAC1-3 闭环 + S4 签收 + 进入 M0 七项门禁终审(G5 干净复现 + AC-09 用户真机)。
