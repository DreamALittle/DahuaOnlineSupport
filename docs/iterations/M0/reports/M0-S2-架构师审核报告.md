# SPRINT M0-S2 架构师审核报告

- 审核人:架构师 Agent | 日期:2026-09-21 | 复核对象:`iter/m0` @ `36915f1`
- 特别说明:架构师在本机(150% 缩放桌面)亲自执行了 QA 走查手册的 L2 验证以获取第一手证据(一次性验证脚本,不入仓);证据已按 QA 命名规范落 `qa/evidence/M0-S2-l2/results/`。

## 1. 材料齐备性

三份《任务完成报告》齐备(DevA `76f6d31` / DevB `bf9080b` / QA `36915f1`)。RJ-S1-03/04/05 均已修复并由 QA 复测通过(UT 恢复原期望)。

## 2. 四项门禁结论

| 门禁 | 结果 | 复核证据 |
|---|---|---|
| ① 复跑 build/test | **PASS** | build 0 警告 0 错误;57/57 测试通过(266ms) |
| ② 红线快扫 | **PASS** | 禁止 API 零命中(仅注释说明);Input P/Invoke 严格限于白名单(EnumWindows 系 + GetClientRect/ClientToScreen);无 PostMessage 实现提前(仅帮助文本标注 [S4]) |
| ③ 范围核对 | **PASS** | Vision 仍为骨架;S2-1/2/3/4 全部落地且无 S3+ 越界;CPM(Directory.Packages.props)落地,YamlDotNet 统一 16.3.0 |
| ④ SAC 核对 | **FAIL(1 项阻塞缺陷 + 1 项环境前置未满足)** | 见第 3/4 节 |

## 3. 阻塞缺陷(本轮唯一不通过原因)

**DEF-S2-01(阻塞,指派 Dev A)**:`dh2ctl` 任何子命令解析仓库自带 `configs/dev.yaml` 即失败——`ConfigLoader.Parse` 抛 `InvalidDataException`(YamlDotNet 反序列化异常,定位 targets 列表项)。根因:`DevConfig`/`WindowTargetConfig`/`PathConfig`/`MatchingConfig`/`InputConfig` 仍为 init-only 属性/位置参数 record,与 S1 的 DEF-S1-01 同类(RJ-S1-03 修复了 MockLayout 族,遗漏 DevConfig 族)。**后果:IT-01/IT-02 无法执行,SAC2-1/SAC2-2 不可验证。**

**RJ-S2-01(Dev A)**:在 `dev-a/m0-s2` 追加提交——①按 RJ-S1-03 先例将 DevConfig 族改为无参构造 class(settable 属性);②新增回归 UT:**直接解析仓库真实文件** `configs/dev.yaml` 与 `configs/mock-layout.yaml`(防再犯);③全量 build/test/format 绿后推送,commit 注明 `fix: RJ-S2-01`。

## 4. SAC1-3 走查实录(架构师代跑,150% 缩放环境)

| 项 | 结果 |
|---|---|
| 窗口发现/句柄/客户区 | ✅ 800×600,geometry 正确 |
| 启动状态文件 | ✅ state.json 原子写,Idle/counter=0 |
| 双路日志 | ✅ raw(0x200/0x201/0x202)与 ui(PointerPressed/Released)同文件双写 |
| 状态机正向转移 | ✅ 第一次注入点击驱动 Idle→Pathfinding→(2s)→Arrived,时序与 mock-layout stateTimings 一致 |
| 返回转移(第二次点击) | ❌ 未触发——**环境根因:本机显示缩放 150%,违反走查手册 100% 前置**;合成注入跨 DPI 空间坐标不确定(实测 raw 坐标 = 注入坐标 ×1.5),两次重试(含 1.5 系数修正)均无法稳定命中按钮 |

**裁定**:SAC1-3 的"点击→转移"剩余部分,合并到 **S4 的 IT-04/e2e** 闭环——S4 的 `dh2ctl click` 用 PostMessage 直接投递客户区坐标(带 lParam,无光标、无 DPI 换算),对该断言是确定性路径,比 100% 缩放人工冒烟更强。原"S2 不通过则 SAC1-3 未闭环"条件,修订为:**SAC1-3 必须在 S4 审核前闭环(e2e 方式),否则 S4 不通过**。100% 缩放人工冒烟降级为可选补充。

## 5. 非阻塞记录

- MockGame raw 日志在非 100% 缩放下记录虚拟化坐标(×1.5),README 加一句"日志坐标为应用逻辑坐标,非物理像素"即可(QA 顺手);
- 走查手册补一步"结束确认 DH2.MockGame 进程已退出"(本轮实测存在孤儿进程残留风险);
- `Input`/`Capture` 行覆盖 0% 符合预期(Win32/GDI 薄层,由 IT-01/02 集成覆盖)。

## 6. 结论

**CHANGES_REQUIRED** —— 唯一阻塞项 DEF-S2-01(RJ-S2-01)。修复推送 + QA 复测后,架构师对 S2 快速复审(仅核对 RJ-S2-01 与 IT-01/02),通过即签发 S3 放行。Dev B 本轮无整改项。
