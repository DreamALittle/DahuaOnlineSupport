# SPRINT M0-S2 架构师审核报告(第三轮·终审)

- 审核人:架构师 Agent | 日期:2026-09-21 | 复核对象:`iter/m0` @ `26d3b09`
- 前轮:第一轮 CHANGES_REQUIRED(DEF-S2-01)→ 第二轮 CHANGES_REQUIRED(DEF-S2-02,新发现)→ 本轮终审。

## 1. 整改闭环核验(架构师真机实测)

| 项 | 结果 |
|---|---|
| DEF-S2-01(RJ-S2-01,第二轮已验) | ✅ 关闭。回归 UT 真读仓库 yaml,enumerate 实测正常 |
| DEF-S2-02(RJ-S2-02/03) | ✅ **真机关闭**:capture 真实句柄 10/10 帧、exit 0、无崩溃(mean=41.1ms/p95=74.1ms);`--hwnd 0` → usage error + exit 2。根因修复正确:ToMat 共享内存→Clone 独立副本(RJ-S2-03);Frame 尺寸构造期缓存 + 空帧跳过 ImWrite + 5 个生命周期回归 UT(RJ-S2-02) |
| IT-01 / SAC2-1 | ✅ 功能通过(enumerate 恰好 1 行 MockGame;1200×900 为 150% 缩放物理像素,100% 下即 800×600,测试设计 §2 环境偏差不作缺陷) |
| IT-02 / SAC2-2 | ✅ **架构师真机全量执行**:10 帧全非黑,尺寸正确,统计输出,证据 `qa/evidence/M0-S2-l2/results/04-capture-pngs/` |
| SAC2-3 | ✅ 大部通过;`--count 0/负数` 静默回退 → DEF-S2-03(见 §3) |
| 门禁①②③ | ✅ build 0W/0E、64/64 全绿、Core 96.52%;红线增量零命中;范围合规 |

## 2. 结论

**PASS(附一项折叠整改)** —— S2 签收。SAC2-1~SAC2-4 全部可判通过;DEF-S2-03 折叠为 S3 开工首项(RJ-S2-04)。SAC1-3 按 S2 第一轮裁定维持"S4 e2e 闭环"条件不变。

## 3. 折叠整改(不阻塞 S3,但为 S3 审核 S 项)

**RJ-S2-04(Dev A)**:`capture --count 0/负数/非整数` 与 `--interval-ms` 非法值当前静默回退默认值、退出 0;应与 `--hwnd` 一致报 usage error 并退出码 2。落点:`dev-a/m0-s3` 分支首笔提交,commit 注明 `fix: RJ-S2-04`。

## 4. 下一轮放行指令(S3)

- 统一触发语:`开始 M0-S3`
- 特别事项:①Dev A 首笔提交落 RJ-S2-04;②S3 范围 = Story 卡 S3-1~S3-4(TemplateStore/TemplateMatcher/save-template/match/资产与金样本)+ UT-03/05/07 + IT-03;严禁越界 S4(PostMessage/click/e2e);③S3-4 资产生成需真机 GUI(架构师或用户桌面会话配合);④IT-03 定位精度断言在 150% 缩放下按测试设计 §2 处理(以 mock-layout 真值的逻辑坐标断言,金样本为 100% 基准则须标注生成条件)。
