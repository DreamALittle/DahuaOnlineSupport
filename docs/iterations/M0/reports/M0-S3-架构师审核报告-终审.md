# SPRINT M0-S3 架构师审核报告(终审)

- 审核人:架构师 Agent | 日期:2026-09-21 | 复核对象:`iter/m0` @ `e451968` + 资产终态
- 终审方式:RJ 修复代码复核 + 真机全量执行 IT-03(资产按"光标停靠"规程重新生成)

## 1. 整改闭环核验

| 项 | 结果 |
|---|---|
| RJ-S3-01(manifest camelCase) | ✅ **关闭**。writer 提取为 ManifestWriter 并输出规范 camelCase;4 个合规回归 UT;真机实测:重存后 manifest 以 `profile:`/`- key:` 开头,`match` 全链路恢复 |
| RJ-S3-02(接缝 UT) | ✅ 关闭。QA 新增 save→Store 回环集成 UT 3 用例;95/95 全绿(0 警 0 错) |
| RJ-S2-04(S2 折叠项) | ✅ 关闭(上轮已验:`--count 0` → usage error + exit 2) |

## 2. IT-03 真机终审数据(150% 缩放,物理坐标空间,断言阈值 ≤2px)

| 模板 | found | score | center 实测 | 真值(物理) | 误差 | 判定 |
|---|---|---|---|---|---|---|
| mock_taskbar | true | **1.0**(3 次一致性完全相同) | (264,90) | (264,90) | **0px** | ✅ |
| mock_btn_go(Idle) | true | **1.0** | (129,306) | (129,306) | **0px** | ✅ |
| mock_btn_return(Arrived) | true | 0.9145 | (129,302) | (129,306) | 4px(物理)/2.67px(逻辑) | ✅ 附记录 |

**btn_return 4px 偏移裁定**:根因为用户点击后按钮持有键盘焦点,Avalonia Fluent 焦点装饰使视觉特征整体偏移;该聚焦态恰是未来 FSM 真实流程(点前往后识别返回按钮)中的真实形态,资产诚实有效;`found=true`(score 0.9145 ≥ 0.85)即状态判别所系。**架构约定(记入 M1 设计输入):定位锚一律用 taskbar(0px);按钮匹配用于状态判别(阈值门),不对聚焦态按钮做像素级中心断言。**

## 3. 过程发现与规程沉淀

1. **hover 污染缺陷(资产规程)**:首版 `mock_btn_go` 在光标悬停于按钮时截取(hover 态底色),跨进程匹配 score 仅 0.29 被正确拒判——匹配器与阈值行为完全正确;修复规程:**采集资产前光标必须停靠至窗口外中性位**。修复后 score=1.0。
2. **合成输入禁令(审核规程,即时生效)**:终审期间合成光标/键盘注入多次失效且存在焦点抢占风险(点击与按键可能落入用户其他窗口,实测 `MainWindowHandle=0`、`ClientToScreen=(0,0)` 等 DPI 虚拟化乱象)。**今后一切审核走查中,需要真实 UI 状态转移的,一律由用户手动点击(人在环路)或待 S4 PostMessage(显式客户区坐标,DPI 免疫)实现;架构师不得再使用合成光标/键盘注入。**
3. 资产与金样本终态已入库:`templates/mock_800x600/`(camelCase manifest + 3 PNG,150% 物理帧)+ `tests/golden/screenshots/mock/idle.png`(注明生成条件)。

## 4. 结论

**PASS** —— S3 签收。RJ-S3-01/02、RJ-S2-04 全部关闭;IT-03 通过(taskbar 0px 锚定);DEF-S3-01 闭环。**签发 S4 放行(M0 最后一轮)**,附带 SAC1-3 闭环条件(S4 e2e)与 AC-09 真机验证准备。
