# SPRINT M0-S4 架构师审核报告(第三轮)

- 审核人:架构师 Agent | 日期:2026-09-21 | 复核对象:`iter/m0` @ `783f8f2`

## 1. RJ-S4-03 闭环核验

✅ **DEF-S4-02 关闭**:轮询谓词修正真机生效——`state observed: Arrived`(elapsed 2425ms,与 2s 寻路时序精确吻合);152/152 全绿(+11 谓词语义 UT)。

## 2. 新发现两项

**DEF-S4-03(阻塞,指派 Dev B)**:Arrived 态"返回"按钮未渲染背景。证据:`e2e-20260921174609/frame_after.png`——状态"已到达目的地",按钮区域仅白色"返回"文字,无 #2D5BFF 底色;而 Idle 态"前往"按钮底色正常。技术设计 §7 规定前往/返回同款样式。**无论输入路径(posted click / 真实点击)如何,同一 UI 状态必须渲染一致**。请 Dev B 排查 MainWindow 按钮背景绑定(疑似状态切换时 Background 绑定丢失/触发器缺失)。

**DEF-S3-02 确认与扩大(资产污染)**:复核发现 `mock_btn_return.png`(12:02 版)亦为污染资产(浅灰游戏场景裁剪,非蓝底按钮)——S3 终审的 0.9145 同为自指假阳性。至此 10:40–14:10 间生成的全部资产判定作废;14:19 后按独立像素断言规程生成的 taskbar/btn_go/gold-idle 有效。

## 3. 修复与防线(RJ-S4-04 / RJ-S4-05)

- **RJ-S4-04(Dev B)**:修复 DEF-S4-03 按钮 Arrived 态背景;补一个 MockGame 视觉状态 UT(XAML 逻辑层可测范围)或以截图断言方式自证两种状态按钮均有 #2D5BFF 底色。
- **RJ-S4-05(测试 Agent,永久防线)**:新增**资产完整性守护 UT**——读取 `templates/mock_800x600/png/` 三模板,断言像素签名:taskbar 平均色≈#1E1E2E(深藏青),btn_go/btn_return 中心区主色≈#2D5BFF(蓝)且非灰白;金样本 idle.png 断言四角+任务栏位置像素签名。**被污染的资产从此无法静默入仓。**

## 4. 积极结论

e2e 全链路已通 2.5/3:锚定 1.0 → 帧空间点击 → Arrived 到达(时序精确)→ 仅剩按钮视觉一致性。修复后预期一次通过。

## 5. 结论

**CHANGES_REQUIRED(DEF-S4-03;RJ-S4-04/05)**。完成后由架构师以 `dh2ctl click`(现已可用,无需人工点击)重建 btn_return 资产并终验 e2e。
