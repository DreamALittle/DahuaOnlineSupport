# SPRINT M0-S3:模板匹配与模板库

| 项 | 内容 |
|---|---|
| 目标 | 模板入库(manifest 幂等登记)并在截图上精确定位目标区域(误差 ≤2px) |
| 依赖 | M0-S2 |
| 预估 | 0.5~1 天 |

## Stories

| ID | Story | 负责人 | 技术设计引用 | 测试引用 |
|----|-------|--------|--------------|----------|
| S3-1 | DH2.Vision:TemplateStore(manifest YAML 解析、聚合校验、热重载、不可变快照) | Dev B | 技术设计 §5.1 | UT-03 |
| S3-2 | TemplateMatcher(灰度 + TM_CCOEFF_NORMED + ROI 坐标回换算) | Dev B | 技术设计 §5.2 | UT-05、UT-07(合成图部分) |
| S3-3 | dh2ctl:`save-template`(截屏裁剪入库 + manifest 登记幂等)与 `match` 命令(JSON 行输出) | Dev A | 技术设计 §6 | 手动验证 + IT-03 |
| S3-4 | 生成并提交初始资产:模板 `mock_taskbar`/`mock_btn_go`/`mock_btn_return` + 金样本 `tests/golden/screenshots/mock/idle.png`(生成条件注明)——用 dh2ctl 生成后入库 | 测试 Agent(工具由 S3-3 提供) | 技术设计 §10.5 | UT-07(金样本部分) |

## SAC(Sprint 验收标准,Review 逐条核对)

- [ ] SAC3-1 UT-03/05/07 全绿(manifest 校验聚合报错、阈值判定、金样本定位)
- [ ] SAC3-2 IT-03 通过:`match mock_taskbar` 定位中心与 mock-layout 真值(176,60)误差 ≤2px
- [ ] SAC3-3 `save-template` 对同名 key 重复执行为幂等覆盖,manifest 保留有效条目
- [ ] SAC3-4 模板 PNG 与金样本已提交入库,manifest 与文件一一对应

## 范围外(严禁实施)

- PostMessageDriver/click/e2e/report 命令(S4)
- OCR、ROI 锚点体系、场景分类器(属 M1a,即使看起来"顺手"也不做)

## 轮次完成报告(各角色填写,收口材料)

- Dev A → `../reports/M0-S3-DevA-任务完成报告.md`
- Dev B → `../reports/M0-S3-DevB-任务完成报告.md`
- QA → `../reports/M0-S3-QA-任务完成报告.md`(含集成记录/用例矩阵/覆盖率/DEF/资产/结论)

三份齐备并全部进入 iter/m0 后,架构师才启动集中审查。

## 审核记录(架构师填写)

(结论与整改项见 `../reports/M0-S3-架构师审核报告.md`;通过后由架构师签发下一轮放行指令,未收到放行指令不得开始下一 Sprint)
