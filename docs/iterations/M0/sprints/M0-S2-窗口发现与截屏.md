# SPRINT M0-S2:窗口发现与截屏

| 项 | 内容 |
|---|---|
| 目标 | 能按配置枚举并绑定目标窗口;能对指定窗口客户区稳定连续截屏 |
| 依赖 | M0-S1 |
| 预估 | 0.5~1 天 |

## Stories

| ID | Story | 负责人 | 技术设计引用 | 测试引用 |
|----|-------|--------|--------------|----------|
| S2-1 | DH2.Input:NativeMethods(EnumWindows 系/GetClientRect/ClientToScreen 白名单声明)+ WindowEnumerator(ProcessName 经 `Process.GetProcessById`,禁止声明 OpenProcess) | Dev B | 技术设计 §3.1、§3.2 | IT-01 |
| S2-2 | DH2.Capture:GdiCapture(CopyFromScreen 按客户区屏幕矩形)+ WgcCapture 占位(NotImplementedException) | Dev B | 技术设计 §4 | IT-02 |
| S2-3 | dh2ctl CLI 骨架:手动参数解析、退出码约定(0/2/3)、Serilog(console+file) | Dev A | 技术设计 §6、§9 | — |
| S2-4 | `enumerate` 与 `capture` 两命令集成(消费 S2-1/S2-2 的接口实现) | Dev A(依赖 S2-1~S2-3) | 技术设计 §6 | IT-01、IT-02 |

## SAC(Sprint 验收标准,Review 逐条核对)

- [ ] SAC2-1 IT-01 通过:MockGame 运行时 `dh2ctl enumerate` 恰好列出 1 条,Title/ProcessName/客户区 Rect 正确(800×600)
- [ ] SAC2-2 IT-02 通过:连续 10 帧截图尺寸正确、非黑帧、输出均值/p95 耗时
- [ ] SAC2-3 CLI 行为:非法参数退出码 2;配置校验失败聚合输出全部错误且退出码 3;`--config` 缺省指向 `configs/dev.yaml`
- [ ] SAC2-4 build/test/format 依旧全绿,无越界实现(未做匹配与点击)

## 范围外(严禁实施)

- TemplateStore/TemplateMatcher(S3);PostMessageDriver、click/e2e 命令(S4)
- save-template/match 命令(S3)

## 轮次完成报告(各角色填写,收口材料)

- Dev A → `../reports/M0-S2-DevA-任务完成报告.md`
- Dev B → `../reports/M0-S2-DevB-任务完成报告.md`
- QA → `../reports/M0-S2-QA-任务完成报告.md`(含集成记录/用例矩阵/覆盖率/DEF/资产/结论)

三份齐备并全部进入 iter/m0 后,架构师才启动集中审查。

## 审核记录(架构师填写)

(结论与整改项见 `../reports/M0-S2-架构师审核报告.md`;通过后由架构师签发下一轮放行指令,未收到放行指令不得开始下一 Sprint)
