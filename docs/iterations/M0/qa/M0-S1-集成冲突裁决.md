# S1 集成冲突裁决书 RJ-M0-S1

- 裁决人:架构师 Agent | 日期:2026-09-20
- 背景:dev-a/m0-s1(HEAD 8a87d11,merge-base ee011da)与 iter/m0(已含 Dev B 的 S1-3 MockGame 完整实现)存在逻辑冲突。QA 上报属实并缓执行"出现即合入",行为正确,予以肯定;架构师此前基于旧快照的进度消息过时,引为教训:**此后一切状态类指令,执行方一律以 git 实况核验为准,指令与实况不符时以上报代替执行**。

## 裁决 1:文件归属(MockGame)

`tools/DH2.MockGame/` 全部实现文件(14 个,含 Models/Services/Views/ViewModels)与 `configs/mock-layout.yaml` **一律以 iter/m0(Dev B S1-3)为准**。Dev A 骨架中的 MockGame 空壳文件与 Dev B 实现重叠者,在合并中**丢弃 Dev A 侧**;DH2.sln 保留 Dev A 版本,但项目引用必须指向 Dev B 的 MockGame csproj,并包含 Dev B 引入的全部源文件。

## 裁决 2:Avalonia 版本统一

全解决方案统一 **Avalonia 11.2.x**(以 Dev A 骨架所引 11.2.7 为准)。依据:02 文档选型与 README 决策记录均为"Avalonia 11";Dev B 使用 12.1.2 属**技术设计偏离**,其未在报告中声明,记整改项。MockGame 使用的 API(Window/Button/Pointer 事件/Dispatcher)在 11 与 12 间无破坏性差异,降级成本可控;若 Dev B 代码存在 12 专属 API 无法编译,由 Dev B 修复并说明。

## 整改项(按 07 §5 类似 DEF 的方式跟踪)

| 编号 | 指派 | 内容 | 关闭条件 |
|---|---|---|---|
| RJ-S1-01 | Dev A | 在 dev-a/m0-s1 上 merge origin/iter/m0,按裁决 1 解决全部冲突,确保合并后 sln 完整引用 6+1 项目且 MockGame 为 Dev B 实现;全量 `dotnet build -c Release` 零警告零错误后推送 | QA 合入后全量构建通过 |
| RJ-S1-02 | Dev B | 将 MockGame 的 Avalonia 包从 12.1.2 降至 11.2.7 并适配(如有 12 专属 API 一并修改),在 dev-b/m0-s1 修复推送,commit 注明 `fix: RJ-S1-02` | 全量构建通过且 QA 复测 |

## 执行顺序

1. Dev A 完成 RJ-S1-01 推送;Dev B 完成 RJ-S1-02 推送(两者可并行,互不依赖);
2. QA 将两者合入 iter/m0(此处为文本冲突已消解的干净合并;若仍有冲突,冻结并回报架构师,**禁止再次自裁**),执行全量 build + UT-01/02/04/06/08 + MockGame 走查;
3. QA 出 `M0-S1-QA-任务完成报告.md`(含 RJ 两项关闭核验),push iter/m0;
4. 三份报告齐备 → 架构师收口审查。
